{
  "name": "@sensorpack/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "multisite-sensorpack-mvp API契約(OpenAPI)と生成TypeScript型定義。Next.js/Rails/FastAPI/ESP32が参照するsingle source of truth。",
  "scripts": {
    "lint": "redocly lint openapi.yaml",
    "generate:types": "openapi-typescript openapi.yaml -o types/api.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run lint && npm run generate:types && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@redocly/cli": "^2.41.1",
    "js-yaml": "^5.2.2",
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "brace-expansion": "^5.0.8"
  }
}
