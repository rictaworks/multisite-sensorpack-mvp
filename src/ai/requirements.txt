# AI日次サマリー（F7）用FastAPIサービスの依存関係。
#
# .claude/rules/architecture.md（A06: 脆弱または古いライブラリの使用対策）に基づき、
# 既知の安定版をバージョン固定する。更新時はセキュリティアドバイザリを確認すること。
#
# 現時点（Issue #3）のスコープはひな形のみ。実際のLLM呼び出し・
# クォータ制御（requirements.md F7）は今後のIssueで実装する。

fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2

# LangChain（requirements.md 1.3節：AIサマリーはPython + FastAPI + LangChain）。
# LangSmith観測（LANGCHAIN_TRACING_V2等）は langchain 本体の環境変数連携で有効化する。
langchain==1.3.14
langchain-openai==1.4.1

# テスト
pytest==9.1.1
httpx==0.28.1
