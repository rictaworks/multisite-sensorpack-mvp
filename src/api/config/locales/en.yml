# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  hello: "Hello world"
  errors:
    unauthorized: "Authentication is required."
    forbidden: "You do not have permission to access this resource."
    not_found: "The requested resource was not found."
    validation_error: "The request is invalid."
    rate_limited: "Too many requests. Please try again later."
    recaptcha_failed: "reCAPTCHA verification failed."
    claim_code_not_found: "No claim code matches the submitted value."
    claim_code_expired: "This claim code has expired."
    claim_code_used: "This claim code has already been used."
    claim_code_locked: "This claim code was locked after repeated failed attempts."
    invalid_device_token: "The device token is invalid."
    device_deleted: "This device has been deleted."
