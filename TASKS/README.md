# TASKS/

実装単位のタスクを管理するディレクトリ。

- `project-manager` エージェントが `requirements.md` と `app-ui/` からGitHub Issueに分割する際の下書き・記録に使う（詳細は `.claude/agents/project-manager.md`、運用ルールの正は `.claude/Manager.md`）。
- サブプロジェクト作成・CI整備など、GitHub Issue化する前の起票メモもここに置いてよい。
- ファイル名は `YYYY-MM-DD-{概要}.md` を推奨する。
